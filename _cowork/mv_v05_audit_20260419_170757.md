
# MediaVault v0.5 audit

- **Generated:** 2026-04-19T17:07:57
- **DB path:** C:\AI\Platform\MediaVault\core\mediavault.sqlite
- **DB exists:** True
- **DB size (bytes):** 1953792
- **DB mtime:** 2026-04-19T16:41:56

## 1. Build lock

```txt
UNLOCKED
Last session: MediaVault v0.4 refactor (cowork)
Released: 2026-04-17
Released by: cowork
Outcome: SHIPPED — see C:\AI\Platform\MediaVault\_cowork\PHASE_SUMMARY.md
```

## 2. File inventory (top-level + core/)


### Key files

- `mediavault.html` — 66,164 bytes, mtime 2026-04-17T21:51:31
- `fb_candidates.html` — 19,158 bytes, mtime 2026-04-17T21:56:11
- `imgserver.py` — 45,620 bytes, mtime 2026-04-17T21:44:15
- `imgserver_extensions.py` — 13,112 bytes, mtime 2026-04-16T21:32:44
- `ingest_engine.py` — 34,483 bytes, mtime 2026-04-17T21:55:00
- `SPEC.md` — 20,594 bytes, mtime 2026-04-17T22:05:43
- `STATE.md` — 4,634 bytes, mtime 2026-04-17T22:03:04
- `PROJECT.md` — 2,520 bytes, mtime 2026-04-17T21:58:12
- `WORKFLOW.md` — 6,644 bytes, mtime 2026-04-17T22:05:46
- `MEDIAVAULT_V04_DESIGN.md` — 31,630 bytes, mtime 2026-04-17T21:29:30

### Preserved v0.2 reference files

- `hr_manager.html.old_v02` — 72,148 bytes, mtime 2026-04-16T21:55:27
- `imgserver.py.old_v02` — 43,272 bytes, mtime 2026-04-17T21:41:31

### core/ directory listing

- `__pycache__` — 4,096 bytes
- `_test.txt` — 0 bytes
- `analyze_captures.py` — 672 bytes
- `db_setup.py` — 5,234 bytes
- `enrich_helper.py` — 2,167 bytes
- `enrich_queue.json` — 6,793 bytes
- `enrich_results.json` — 34,533 bytes
- `fb_candidates.json` — 3,847 bytes
- `imgserver.py` — 45,620 bytes
- `imgserver.py.old_v02` — 43,272 bytes
- `imgserver_extensions.py` — 13,112 bytes
- `ingest_engine.py` — 34,483 bytes
- `match_screenshots.py` — 3,581 bytes
- `mediavault.sqlite` — 1,953,792 bytes
- `mediavault.sqlite.bak_v04phase1_20260417_213832` — 1,953,792 bytes
- `migrate_to_v04.py` — 23,309 bytes
- `recapture_urls.txt` — 3,525 bytes
- `rethumb.py` — 878 bytes
- `screenshot_match.json` — 2 bytes
- `tag_vocabulary.json` — 2,464 bytes

## 3. Full schema (as it exists on disk)


### index: idx_artifacts_ingest_date

```sql
CREATE INDEX idx_artifacts_ingest_date  ON artifacts(ingest_date)
```

### index: idx_artifacts_parent

```sql
CREATE INDEX idx_artifacts_parent       ON artifacts(parent_artifact_id)
```

### index: idx_artifacts_post_date

```sql
CREATE INDEX idx_artifacts_post_date    ON artifacts(post_date)
```

### index: idx_artifacts_source_url

```sql
CREATE INDEX idx_artifacts_source_url   ON artifacts(source_url)
```

### index: idx_artifacts_status

```sql
CREATE INDEX idx_artifacts_status       ON artifacts(status)
```

### index: idx_artifacts_storage_mode

```sql
CREATE INDEX idx_artifacts_storage_mode ON artifacts(storage_mode)
```

### index: idx_tags_group

```sql
CREATE INDEX idx_tags_group             ON tags(group_name)
```

### index: idx_tags_proposed

```sql
CREATE INDEX idx_tags_proposed          ON tags(is_proposed)
```

### table: artifacts

```sql
CREATE TABLE "artifacts" (
                id                      TEXT PRIMARY KEY,

                source_url              TEXT,
                source_platform         TEXT,
                ingest_source           TEXT,
                ingest_date             DATE NOT NULL,

                storage_mode            TEXT NOT NULL DEFAULT 'vaulted'
                                            CHECK(storage_mode IN ('vaulted','referenced','url_only')),
                local_asset_path        TEXT,
                thumbnail_path          TEXT,
                link_status             TEXT CHECK(link_status IN ('live','dead','local-only')),

                parent_artifact_id      TEXT REFERENCES "artifacts"(id) ON DELETE CASCADE,
                media_type              TEXT,

                post_date               DATE,
                post_date_confidence    TEXT CHECK(post_date_confidence IN
                                            ('extracted','manual','estimated','unknown')),
                capture_date            DATE,

                status                  TEXT NOT NULL DEFAULT 'vault'
                                            CHECK(status IN ('inbox','vault','released','archived')),
                released_at             TEXT,
                released_by             TEXT,

                description_short       TEXT,
                description_long        TEXT,
                extracted_text          TEXT,
                author_name             TEXT,

                tags                    TEXT NOT NULL DEFAULT '[]',

                tags_permission         TEXT,
                permission_contact      TEXT,
                permission_evidence_path TEXT,

                confidence_flags        TEXT,
                notes                   TEXT,
                created_at              TEXT NOT NULL DEFAULT (datetime('now')),
                updated_at              TEXT NOT NULL DEFAULT (datetime('now'))
            )
```

### table: id_sequence

```sql
CREATE TABLE "id_sequence" (
                date_str  TEXT PRIMARY KEY,
                last_seq  INTEGER NOT NULL DEFAULT 0
            )
```

### table: ingest_queue

```sql
CREATE TABLE "ingest_queue" (
                queue_id        INTEGER PRIMARY KEY,
                ingest_source   TEXT NOT NULL,
                raw_path        TEXT,
                source_url      TEXT,
                queued_at       TEXT NOT NULL,
                status          TEXT NOT NULL DEFAULT 'pending'
                                    CHECK(status IN ('pending','keep','skip','enriched','approved','failed')),
                enrichment_json TEXT,
                error_message   TEXT,
                artifact_id     TEXT,
                updated_at      TEXT NOT NULL DEFAULT (datetime('now'))
            )
```

### table: tags

```sql
CREATE TABLE tags (
                slug            TEXT PRIMARY KEY,
                display_name    TEXT NOT NULL,
                description     TEXT,
                group_name      TEXT,
                is_proposed     INTEGER NOT NULL DEFAULT 0,
                usage_count     INTEGER NOT NULL DEFAULT 0,
                created_at      TEXT NOT NULL DEFAULT (datetime('now'))
            )
```

### artifacts column list, in order

| # | name | type | notnull | default | pk |
|---|------|------|---------|---------|----|
| 0 | `id` | TEXT | 0 | None | 1 |
| 1 | `source_url` | TEXT | 0 | None | 0 |
| 2 | `source_platform` | TEXT | 0 | None | 0 |
| 3 | `ingest_source` | TEXT | 0 | None | 0 |
| 4 | `ingest_date` | DATE | 1 | None | 0 |
| 5 | `storage_mode` | TEXT | 1 | 'vaulted' | 0 |
| 6 | `local_asset_path` | TEXT | 0 | None | 0 |
| 7 | `thumbnail_path` | TEXT | 0 | None | 0 |
| 8 | `link_status` | TEXT | 0 | None | 0 |
| 9 | `parent_artifact_id` | TEXT | 0 | None | 0 |
| 10 | `media_type` | TEXT | 0 | None | 0 |
| 11 | `post_date` | DATE | 0 | None | 0 |
| 12 | `post_date_confidence` | TEXT | 0 | None | 0 |
| 13 | `capture_date` | DATE | 0 | None | 0 |
| 14 | `status` | TEXT | 1 | 'vault' | 0 |
| 15 | `released_at` | TEXT | 0 | None | 0 |
| 16 | `released_by` | TEXT | 0 | None | 0 |
| 17 | `description_short` | TEXT | 0 | None | 0 |
| 18 | `description_long` | TEXT | 0 | None | 0 |
| 19 | `extracted_text` | TEXT | 0 | None | 0 |
| 20 | `author_name` | TEXT | 0 | None | 0 |
| 21 | `tags` | TEXT | 1 | '[]' | 0 |
| 22 | `tags_permission` | TEXT | 0 | None | 0 |
| 23 | `permission_contact` | TEXT | 0 | None | 0 |
| 24 | `permission_evidence_path` | TEXT | 0 | None | 0 |
| 25 | `confidence_flags` | TEXT | 0 | None | 0 |
| 26 | `notes` | TEXT | 0 | None | 0 |
| 27 | `created_at` | TEXT | 1 | datetime('now') | 0 |
| 28 | `updated_at` | TEXT | 1 | datetime('now') | 0 |

### tags (vocabulary) column list, in order

| # | name | type | notnull | default | pk |
|---|------|------|---------|---------|----|
| 0 | `slug` | TEXT | 0 | None | 1 |
| 1 | `display_name` | TEXT | 1 | None | 0 |
| 2 | `description` | TEXT | 0 | None | 0 |
| 3 | `group_name` | TEXT | 0 | None | 0 |
| 4 | `is_proposed` | INTEGER | 1 | 0 | 0 |
| 5 | `usage_count` | INTEGER | 1 | 0 | 0 |
| 6 | `created_at` | TEXT | 1 | datetime('now') | 0 |

## 4. Row counts


### Totals by table

- **artifacts:** 80
- **tags:** 106
- **id_sequence:** 4
- **ingest_queue:** 25

### artifacts by status

- **vault:** 77
- **released:** 3

### artifacts by storage_mode

- **referenced:** 60
- **vaulted:** 20

### artifacts by (status, storage_mode) cross

| status | storage_mode | count |
|---|---|---|
| vault | referenced | 60 |
| vault | vaulted | 17 |
| released | vaulted | 3 |

### artifacts with parent_artifact_id set (children)

- **children with a parent link:** 18

### artifacts with released_at set

- **released_at populated:** 3

### ingest_queue by status

- **keep:** 25

## 5. Vestigial / legacy columns on artifacts

Counts how many rows have a non-null value in each column the v0.4 SPEC labels vestigial. Used to decide what to drop in v0.5.
| column | exists on disk | non-null rows | distinct values (sample up to 8) |
|---|---|---|---|
| `link_status` | yes | 76 | `live`, `local-only` |
| `tags_permission` | yes | 36 | `not-requested` |
| `tags_preservation` | **no** | — | — |
| `permission_contact` | yes | 0 | — |
| `permission_evidence_path` | yes | 0 | — |
| `author_name` | yes | 37 | `(1) Video`, `Hunter Root`, `(2) Video`, ``, `hunterrootofficial`, `ElmThree Productions` |
| `confidence_flags` | yes | 0 | — |
| `media_type` | yes | 73 | `video`, `text-only`, `mixed`, `photo`, `link` |
| `capture_date` | yes | 76 | `2026-04-05`, `2026-04-06`, `2026-04-16`, `2026-04-17` |
| `post_date_confidence` | yes | 80 | `extracted`, `unknown`, `manual`, `estimated` |

## 6. Vocabulary dump — every tag in the `tags` table

- **vocabulary total:** 106
- **proposed (is_proposed=1):** 0
- **accepted (is_proposed=0):** 106
- **with group_name:** 6
- **without group_name:** 100

### Every tag, sorted by usage desc

| slug | display_name | group_name | proposed | uses | desc |
|---|---|---|---|---|---|
| `hunter_root` | Hunter Root | — | 0 | 77 |  |
| `standard` | Standard | preservation | 0 | 76 |  |
| `reverbnation` | Reverbnation | — | 0 | 40 |  |
| `pre_solo_run_with_the_hunt` | Pre Solo Run With The Hunt | — | 0 | 33 |  |
| `runwiththehunt` | Runwiththehunt | — | 0 | 33 |  |
| `hunter` | Hunter | — | 0 | 31 |  |
| `audio` | Audio | — | 0 | 28 |  |
| `mp3` | Mp3 | — | 0 | 28 |  |
| `mp3_only` | Mp3 Only | — | 0 | 26 |  |
| `phase2_recovery` | Phase2 Recovery | — | 0 | 26 |  |
| `notable` | Notable | rarity | 0 | 24 |  |
| `2025` | 2025 | — | 0 | 21 |  |
| `show` | Show | — | 0 | 21 |  |
| `live_show` | Live Show | — | 0 | 20 |  |
| `gear` | Gear | — | 0 | 16 |  |
| `audio_audio_file` | Audio Audio File | — | 0 | 15 |  |
| `solo` | Solo | — | 0 | 15 |  |
| `audio_metadata_json` | Audio Metadata Json | — | 0 | 13 |  |
| `band` | Band | — | 0 | 13 |  |
| `personal` | Personal | — | 0 | 13 |  |
| `venue` | Venue | — | 0 | 11 |  |
| `common` | Common | rarity | 0 | 9 |  |
| `new_music` | New Music | — | 0 | 7 |  |
| `rock` | Rock | — | 0 | 7 |  |
| `artist_page` | Artist Page | — | 0 | 6 |  |
| `medusasdisco` | Medusasdisco | — | 0 | 6 |  |
| `pre_solo_medusas_disco` | Pre Solo Medusas Disco | — | 0 | 6 |  |
| `song_page` | Song Page | — | 0 | 6 |  |
| `acoustic_rock` | Acoustic Rock | — | 0 | 5 |  |
| `alternative` | Alternative | — | 0 | 5 |  |
| `grunge` | Grunge | — | 0 | 5 |  |
| `jam` | Jam | — | 0 | 5 |  |
| `pre_release` | Pre Release | — | 0 | 5 |  |
| `psychedelic` | Psychedelic | — | 0 | 5 |  |
| `2023` | 2023 | — | 0 | 4 |  |
| `fan` | Fan | — | 0 | 4 |  |
| `tour` | Tour | — | 0 | 4 |  |
| `tour_announcement` | Tour Announcement | — | 0 | 4 |  |
| `milestone` | Milestone | — | 0 | 3 |  |
| `reverbnation_artist_page_metadata_json` | Reverbnation Artist Page Metadata Json | — | 0 | 3 |  |
| `reverbnation_artist_page_page_save_html` | Reverbnation Artist Page Page Save Html | — | 0 | 3 |  |
| `singer_songwriter` | Singer Songwriter | — | 0 | 3 |  |
| `town_rat_heathen` | Town Rat Heathen | — | 0 | 3 |  |
| `2022` | 2022 | — | 0 | 2 |  |
| `2024` | 2024 | — | 0 | 2 |  |
| `acoustic` | Acoustic | — | 0 | 2 |  |
| `bedroom_recording` | Bedroom Recording | — | 0 | 2 |  |
| `blues` | Blues | — | 0 | 2 |  |
| `distrokid` | Distrokid | — | 0 | 2 |  |
| `dreaming_up_ways_of_gettin_outta_this_hellhole` | Dreaming Up Ways Of Gettin Outta This Hellhole | — | 0 | 2 |  |
| `hellhole_perspective_rubble_lyrics` | Hellhole Perspective Rubble Lyrics | — | 0 | 2 |  |
| `hunterroot2` | Hunterroot2 | — | 0 | 2 |  |
| `indieartist` | Indieartist | — | 0 | 2 |  |
| `indiefolk` | Indiefolk | — | 0 | 2 |  |
| `lyme_disease` | Lyme Disease | — | 0 | 2 |  |
| `promotional_post` | Promotional Post | — | 0 | 2 |  |
| `quicksand_sinking` | Quicksand Sinking | — | 0 | 2 |  |
| `rare` | Rare | rarity | 0 | 2 |  |
| `released` | Released | — | 0 | 2 |  |
| `reverbnation_song_page_lyrics_txt` | Reverbnation Song Page Lyrics Txt | — | 0 | 2 |  |
| `reverbnation_song_page_metadata_json` | Reverbnation Song Page Metadata Json | — | 0 | 2 |  |
| `reverbnation_song_page_page_save_html` | Reverbnation Song Page Page Save Html | — | 0 | 2 |  |
| `song` | Song | — | 0 | 2 |  |
| `tiktok` | Tiktok | — | 0 | 2 |  |
| `unique` | Unique | rarity | 0 | 2 |  |
| `100k_followers` | 100K Followers | — | 0 | 1 |  |
| `2019` | 2019 | — | 0 | 1 |  |
| `2026` | 2026 | — | 0 | 1 |  |
| `acoustic_guitar` | Acoustic Guitar | — | 0 | 1 |  |
| `acoustic_performance` | Acoustic Performance | — | 0 | 1 |  |
| `advance_tickets` | Advance Tickets | — | 0 | 1 |  |
| `brick_wall` | Brick Wall | — | 0 | 1 |  |
| `brick_wall_background` | Brick Wall Background | — | 0 | 1 |  |
| `cover_song` | Cover Song | — | 0 | 1 |  |
| `critical` | Critical | preservation | 0 | 1 |  |
| `crooked_home` | Crooked Home | — | 0 | 1 |  |
| `early_stages` | Early Stages | — | 0 | 1 |  |
| `early_version` | Early Version | — | 0 | 1 |  |
| `event_listing` | Event Listing | — | 0 | 1 |  |
| `fall_tour` | Fall Tour | — | 0 | 1 |  |
| `family` | Family | — | 0 | 1 |  |
| `hacked_account` | Hacked Account | — | 0 | 1 |  |
| `instagram` | Instagram | — | 0 | 1 |  |
| `instagram_hacked` | Instagram Hacked | — | 0 | 1 |  |
| `lancaster_pa` | Lancaster Pa | — | 0 | 1 |  |
| `long_hair` | Long Hair | — | 0 | 1 |  |
| `loss` | Loss | — | 0 | 1 |  |
| `lyric` | Lyric | — | 0 | 1 |  |
| `medusas_disco` | Medusas Disco | — | 0 | 1 |  |
| `mental_health` | Mental Health | — | 0 | 1 |  |
| `merch` | Merch | — | 0 | 1 |  |
| `my_brothers_bones` | My Brothers Bones | — | 0 | 1 |  |
| `new_song` | New Song | — | 0 | 1 |  |
| `nick_root` | Nick Root | — | 0 | 1 |  |
| `original_music` | Original Music | — | 0 | 1 |  |
| `performance` | Performance | — | 0 | 1 |  |
| `poster` | Poster | — | 0 | 1 |  |
| `psychedelic_rock` | Psychedelic Rock | — | 0 | 1 |  |
| `quote` | Quote | — | 0 | 1 |  |
| `rehearsal` | Rehearsal | — | 0 | 1 |  |
| `social` | Social | — | 0 | 1 |  |
| `songwriting` | Songwriting | — | 0 | 1 |  |
| `songwriting_process` | Songwriting Process | — | 0 | 1 |  |
| `striped_shirt` | Striped Shirt | — | 0 | 1 |  |
| `ticketing` | Ticketing | — | 0 | 1 |  |
| `tribute` | Tribute | — | 0 | 1 |  |

## 7. Vocabulary pollution scan

Flags for Mike to eyeball during v0.5 cleanup. No automated merge; the conservative cleanup script only acts on items Mike explicitly approves.

### 7a. Near-duplicate buckets (same stem)

- stem `medusasdisco`:
  - `medusasdisco` → Medusasdisco (6 uses)
  - `medusas_disco` → Medusas Disco (1 uses)

### 7b. Zero-usage tags (candidates for bulk delete)

- **zero-use count:** 0

### 7c. Single-use tags (curator eyeball — often descriptive one-offs)

- **single-use count:** 41
```
100k_followers
2019
2026
acoustic_guitar
acoustic_performance
advance_tickets
brick_wall
brick_wall_background
cover_song
critical
crooked_home
early_stages
early_version
event_listing
fall_tour
family
hacked_account
instagram
instagram_hacked
lancaster_pa
long_hair
loss
lyric
medusas_disco
mental_health
merch
my_brothers_bones
new_song
nick_root
original_music
performance
poster
psychedelic_rock
quote
rehearsal
social
songwriting
songwriting_process
striped_shirt
ticketing
tribute
```

### 7d. Slugs that look like visual details (pattern match)

_Match against a hand-written list of physical-descriptor keywords. Not exhaustive._
- `blues` → Blues (2 uses)
- `long_hair` → Long Hair (1 uses)
- `striped_shirt` → Striped Shirt (1 uses)

### 7e. Slugs with spaces/capitals/weird chars (slug rule violations)

_(none — all slugs look well-formed)_

## 8. ingest_queue deep inspection

- **queue rows total:** 25

### 8a. Status × artifact_id matrix (detects released-in-inbox bug)

If any `status='pending'` row has `artifact_id IS NOT NULL`, that row has already been saved to vault but the queue entry wasn't cleared. That's the bug.

| status | has artifact_id | count |
|---|---|---|
| keep | no | 21 |
| keep | yes | 4 |

### 8b. Specific stuck rows (status='pending' AND artifact_id IS NOT NULL)

_(none — bug has not produced leftover data, or has been cleaned)_

### 8c. Full queue roster (one-line per row)

| q# | status | ingest_source | artifact_id | raw_path | source_url | enr_len | error |
|---|---|---|---|---|---|---|---|
| 179 | keep | local-drop | — | `…ke\processed\20251010_230323763_iOS.heic` | `` | 969 |  |
| 180 | keep | local-drop | — | `…ke\processed\20251010_230329057_iOS.heic` | `` | 922 |  |
| 181 | keep | local-drop | — | `…ke\processed\20251010_234223618_iOS.heic` | `` | 901 |  |
| 182 | keep | local-drop | — | `…ke\processed\20251010_235717309_iOS.heic` | `` | 892 |  |
| 183 | keep | local-drop | — | `…ke\processed\20251010_235719641_iOS.heic` | `` | 926 |  |
| 184 | keep | local-drop | — | `…ke\processed\20251011_010746144_iOS.heic` | `` | 910 |  |
| 185 | keep | local-drop | — | `…ke\processed\20251011_010747360_iOS.heic` | `` | 935 |  |
| 186 | keep | local-drop | — | `…ke\processed\20251011_010758038_iOS.heic` | `` | 899 |  |
| 187 | keep | local-drop | — | `…ke\processed\20251011_010758567_iOS.heic` | `` | 891 |  |
| 188 | keep | local-drop | — | `…ke\processed\20251011_011805831_iOS.heic` | `` | 1016 |  |
| 191 | keep | local-drop | — | `…ke\processed\20251011_011810953_iOS.heic` | `` | 867 |  |
| 199 | keep | local-drop | — | `…ke\processed\20251011_011812587_iOS.heic` | `` | 896 |  |
| 205 | keep | local-drop | MV-20260419-001 | `…ke\processed\20251011_011813745_iOS.heic` | `` | 934 |  |
| 207 | keep | local-drop | — | `…ake\processed\20251011_011904000_iOS.MOV` | `` | 449 |  |
| 213 | keep | local-drop | — | `…ke\processed\20251011_015659410_iOS.heic` | `` | 867 |  |
| 214 | keep | local-drop | — | `…ke\processed\20251011_015700912_iOS.heic` | `` | 982 |  |
| 220 | keep | local-drop | — | `…ke\processed\20251011_015704017_iOS.heic` | `` | 889 |  |
| 223 | keep | local-drop | — | `…ke\processed\20251011_025844348_iOS.heic` | `` | 879 |  |
| 225 | keep | extension-capture | — | `…mages\mv-capture-2026-04-05T19-31-37.png` | `https://www.facebook.com/hunter.root.7/p…` | 1459 |  |
| 265 | keep | extension-capture | — | `…mages\mv-capture-2026-04-05T19-55-03.png` | `https://www.facebook.com/hunter.root.7/p…` | 903 |  |
| 268 | keep | local-drop | — | `…ntake\drop\HOMESTEAD_Reboot_Complete.jpg` | `` | 936 |  |
| 269 | keep | extension-capture | — | `…mages\mv-capture-2026-04-06T02-08-14.png` | `https://www.instagram.com/p/DAHCaeoPtYI/` | 2096 |  |
| 270 | keep | local-drop | MV-20260419-002 | `…ke\drop\Screenshot 2026-04-05 221637.png` | `` | 1166 |  |
| 291 | keep | local-drop | MV-20260419-004 | `…a38845a0a8315d5bb92431db0b7d48374ded.jpg` | `` | 0 |  |
| 294 | keep | local-drop | MV-20260419-003 | `…2495875__cover_art__1329068837_jjjjj.jpg` | `` | 0 |  |

## 9. Parent/child structure

- **total artifacts:** 80
- **with parent_artifact_id:** 18
- **top-level (parent NULL):** 62

### 9a. Artifacts that look like sidecars by filename but have no parent link

- **unlinked sidecar candidates:** 3
- **`MV-HR-20260405-009`:** asset=`C:\AI\Platform\MediaVault\catalogs\hunter_root\_thumbs\MV-HR-20260405-009.jpg` media_type=`text-only` tags=["2025", "fan", "hunter", "hunter_root", "lyme_disease", "notable", "personal", "show", "standard", "tour_announcement"]
- **`MV-HR-20260416-007`:** asset=`C:\AI\Projects\Hunter Root\archive\reverbnation\medusasdisco\park-bench-pigeons_19361978\lyrics.txt` media_type=`text-only` tags=["alternative", "hunter_root", "medusasdisco", "pre_solo_medusas_disco", "psychedelic", "reverbnation", "reverbnation_song_page_lyrics_txt", "rock", "song_page", "standard"]
- **`MV-HR-20260416-013`:** asset=`C:\AI\Projects\Hunter Root\archive\reverbnation\runwiththehunt\park-bench-pigeons_12495875\lyrics.txt` media_type=`text-only` tags=["acoustic_rock", "grunge", "hunter_root", "jam", "pre_solo_run_with_the_hunt", "reverbnation", "reverbnation_song_page_lyrics_txt", "runwiththehunt", "song_page", "standard"]

## 10. enrichment_json shape — 3 sample blobs

We need the exact key names currently being written so v0.5's confidence-tier emission doesn't collide with existing fields.

### queue #270 (status=keep)

```json
{
  "description_short": "Facebook reel featuring puppet character created by ElmThree Productions",
  "description_long": "Facebook reel posted by ElmThree Productions featuring a bearded puppet character with red bandana. The post honors @cheechandchong and references their 'Huge fan of Cheech and Chong' fandom, describing it as being about 'our #1st Hunter Root' with original audio.",
  "extracted_text": "ElmThree Productions \u00b7 Follow\n@ElmThree Productions \u00b7 Original audio\nA big thanks and honor to have @cheechandchong making a video of our #1st Hunter Root.. Being a huge fan of Cheech and Chong, I couldn't imagine any... See more\n@cheechandchong",
  "author_name": "ElmThree Productions",
  "source_url": "https://www.facebook.com/reel/1137424761619325",
  "post_date": "2025-09-03",
  "post_date_confidence": "manual",
  "source_platform": "facebook",
  "media_type_in_post": "video",
  "link_status": "live",
  "tags_year_era": "[\"2025\"]",
  "tags_content_type": "[\"milestone\"]",
  "tags_subject": "[\"hunter\"]",
  "tags_topic": "[\"personal\",\"merch\"]",
  "tags_release_stage": "[]",
  "tags_rarity": "unique",
  "tags_permission": "not-requested",
  "tags_song_reference": null,
  "tags_keywords": null,
  "notes": null
}
```

### queue #269 (status=keep)

```json
{
  "description_short": "hunterrootofficial\u00a080wHey y'all! I just wanted to pop in and say we've been on the road the last couple weeks so I ha",
  "description_long": "hunterrootofficial\n\u00a0\n80w\nHey y'all! I just wanted to pop in and say we've been on the road the last couple weeks so I haven't been posting as much. Apologies! Will get back on that grind ASAP. I will also say I've also been busy recording a new song and cannot wait to share it soon! \ud83d\ude01 Much love to everyone who's been out to a show on this run. We got more dates, check hunterroot.com/shows for tix. \ud83c\udf9f\ufe0f @bearsvilletheater tonight in New York.\u270c\ufe0f\n.\n.\n#musician #tour",
  "extracted_text": "Hey y'all! I just wanted to pop in and say we've been on the road the last couple weeks so I haven't been posting as much. Apologies! Will get back on that grind ASAP. I will also say I've also been busy recording a new song and cannot wait to share it soon! \ud83d\ude01 Much love to everyone who's been out to a show on this run. We got more dates, check hunterroot.com/shows for tix. \ud83c\udf9f\ufe0f @bearsvilletheater tonight in New York.\n\n\u00a0\n80w\nHey y'all! I just wanted to pop in and say we've been on the road the last couple weeks so I haven't been posting as much. Apologies! Will get back on that grind ASAP. I will also say I've also been busy recording a new song and cannot wait to share it soon! \ud83d\ude01 Much love to everyone who's been out to a show on this run. We got more dates, check hunterroot.com/shows for tix. \ud83c\udf9f\ufe0f @bearsvilletheater tonight in New York.\u270c\ufe0f\n.\n.\n#musician #tour",
  "author_name": "hunterrootofficial",
  "source_url": "https://www.instagram.com/p/DAHCaeoPtYI/",
  "post_date": "2024-09-19",
  "post_date_confidence": "manual",
  "source_platform": "instagram",
  "media_type_in_post": "photo",
  "link_status": "live",
  "tags_year_era": "[\"2024\"]",
  "tags_content_type": "[\"tour-announcement\",\"new-music\"]",
  "tags_subject": "[\"hunter\",\"solo\"]",
  "tags_topic": "[\"tour\",\"new-music\",\"personal\"]",
  "tags_release_stage": "[\"pre-release\"]",
  "tags_rarity": "notable",
  "tags_permission": "not-requested",
  "tags_song_reference": null,
  "tags_keywords": null,
  "notes": null
}
```

### queue #268 (status=keep)

```json
{
  "description_short": "Hunter Root performing acoustic set with band at intimate venue",
  "description_long": "Hunter Root performs an acoustic set seated with guitar and microphone, with drummer visible in background. The stage features vibrant pink and blue lighting, and Hunter Root's custom bass drum is prominently displayed. Text overlay reads 'HOMESTEAD: Reboot complete...' suggesting a venue or series relaunch.",
  "extracted_text": "HOMESTEAD: Reboot complete...",
  "author_name": "",
  "source_url": null,
  "post_date": "2026-04-03",
  "post_date_confidence": "extracted",
  "source_platform": "other",
  "media_type_in_post": "photo",
  "link_status": "live",
  "tags_year_era": "[\"2026\"]",
  "tags_content_type": "[\"live-show\",\"poster\"]",
  "tags_subject": "[\"hunter\",\"band\",\"venue\"]",
  "tags_topic": "[\"show\"]",
  "tags_release_stage": "[]",
  "tags_rarity": "notable",
  "tags_permission": "not-requested",
  "tags_song_reference": null,
  "tags_keywords": null,
  "notes": null
}
```

### Key-name inventory across all queue rows

| key | rows using it |
|---|---|
| `description_short` | 23 |
| `description_long` | 23 |
| `extracted_text` | 23 |
| `author_name` | 23 |
| `source_url` | 23 |
| `post_date` | 23 |
| `post_date_confidence` | 23 |
| `source_platform` | 23 |
| `media_type_in_post` | 23 |
| `link_status` | 23 |
| `tags_year_era` | 23 |
| `tags_content_type` | 23 |
| `tags_subject` | 23 |
| `tags_topic` | 23 |
| `tags_release_stage` | 23 |
| `tags_rarity` | 23 |
| `tags_permission` | 23 |
| `tags_song_reference` | 23 |
| `tags_keywords` | 23 |
| `notes` | 23 |

## 11. Sample artifact rows — one per (status, storage_mode) if possible


### released / vaulted — sample id MV-20260419-001

  - `id`: MV-20260419-001
  - `source_url`: None
  - `source_platform`: local
  - `ingest_source`: local-drop
  - `ingest_date`: 2026-04-19
  - `storage_mode`: vaulted
  - `local_asset_path`: C:\AI\Platform\MediaVault\catalogs\vaulted\2026\04\MV-20260419-001.heic
  - `thumbnail_path`: None
  - `link_status`: None
  - `parent_artifact_id`: None
  - `media_type`: None
  - `post_date`: 2025-10-10
  - `post_date_confidence`: extracted
  - `capture_date`: None
  - `status`: released
  - `released_at`: 2026-04-19T14:30:48
  - `released_by`: mike
  - `description_short`: Hunter Root performing acoustic solo set in Harrisburg, Pennsylvania
  - `description_long`: Hunter Root sits on a stool performing an intimate acoustic guitar set in Harrisburg, Pennsylvania. The stage is bathed in blue atmospheric lighting with LED stage lights visible overhead. His pedalboard and various effects pedals are visib…
  - `extracted_text`: None
  - `author_name`: None
  - `tags`: []
  - `tags_permission`: None
  - `permission_contact`: None
  - `permission_evidence_path`: None
  - `confidence_flags`: None
  - `notes`: None
  - `created_at`: 2026-04-19 18:30:48
  - `updated_at`: 2026-04-19 18:30:48

### vault / vaulted — sample id MV-HR-20260405-003

  - `id`: MV-HR-20260405-003
  - `source_url`: https://www.facebook.com/watch?ref=saved&v=1887755838800880
  - `source_platform`: facebook
  - `ingest_source`: extension-capture
  - `ingest_date`: 2026-04-05
  - `storage_mode`: vaulted
  - `local_asset_path`: C:\AI\Platform\MediaVault\catalogs\hunter_root\_thumbs\MV-HR-20260405-003.jpg
  - `thumbnail_path`: C:\AI\Platform\MediaVault\catalogs\hunter_root\_thumbs\MV-HR-20260405-003.jpg
  - `link_status`: live
  - `parent_artifact_id`: None
  - `media_type`: video
  - `post_date`: 2025-11-02
  - `post_date_confidence`: extracted
  - `capture_date`: 2026-04-05
  - `status`: vault
  - `released_at`: None
  - `released_by`: None
  - `description_short`: Lyme disease is kicking my ass
  - `description_long`: Lyme disease is kicking my ass
  - `extracted_text`: Lyme disease is kicking my ass
  - `author_name`: (1) Video
  - `tags`: ["2025", "hunter", "hunter_root", "lyme_disease", "notable", "personal", "standard"]
  - `tags_permission`: not-requested
  - `permission_contact`: None
  - `permission_evidence_path`: None
  - `confidence_flags`: None
  - `notes`: None
  - `created_at`: 2026-04-18 01:40:58
  - `updated_at`: 2026-04-18 01:40:58

### vault / referenced — sample id MV-HR-20260405-016

  - `id`: MV-HR-20260405-016
  - `source_url`: None
  - `source_platform`: other
  - `ingest_source`: local-drop
  - `ingest_date`: 2026-04-05
  - `storage_mode`: referenced
  - `local_asset_path`: C:\AI\Platform\MediaVault\intake\drop\20251010_230323763_iOS.heic
  - `thumbnail_path`: C:\AI\Platform\MediaVault\catalogs\hunter_root\_thumbs\MV-HR-20260405-016.jpg
  - `link_status`: local-only
  - `parent_artifact_id`: None
  - `media_type`: photo
  - `post_date`: 2025-10-10
  - `post_date_confidence`: extracted
  - `capture_date`: 2026-04-05
  - `status`: vault
  - `released_at`: None
  - `released_by`: None
  - `description_short`: Stage setup at Harrisburg venue with instruments and gear before performance
  - `description_long`: Empty stage at a Harrisburg, Pennsylvania venue on October 10, 2025, showing a complete band setup with drum kit branded 'Allman Rose', guitars, bass, keyboard, amplifiers, and monitors arranged on patterned rugs. Red curtain backdrop and s…
  - `extracted_text`: Allman Rose
EXIT
  - `author_name`: 
  - `tags`: ["2025", "band", "gear", "hunter_root", "live_show", "notable", "show", "standard", "venue"]
  - `tags_permission`: not-requested
  - `permission_contact`: None
  - `permission_evidence_path`: None
  - `confidence_flags`: None
  - `notes`: None
  - `created_at`: 2026-04-18 01:40:58
  - `updated_at`: 2026-04-18 01:40:58

## 12. `author_name` column — inventory before dropping

- **rows with author_name populated:** 18

### Distinct author_name values

- **`Hunter Root`:** 13
- **`ElmThree Productions`:** 2
- **`hunterrootofficial`:** 1
- **`(2) Video`:** 1
- **`(1) Video`:** 1

## 13. `imgserver_extensions.py` structural dump

- **size (chars):** 13106

### Defined functions / methods (def lines)

- `_json_response` (nested)
- `_path_is_inside` (nested)
- `_next_artifact_id` (nested)
- `_infer_media_type` (nested)
- `handle_artifact_register` (nested)
- `validated` (nested)
- `handle_asset_raw` (nested)

### Route / path strings referenced

- `/api/artifact-register`
- `/asset-raw`

### Probable schema references (v0.2 column names)

- **mentions of `domain`:** 15
- **mentions of `tags_year_era`:** 3
- **mentions of `tags_content_type`:** 3
- **mentions of `tags_subject`:** 3
- **mentions of `tags_topic`:** 3
- **mentions of `tags_rarity`:** 3
- **mentions of `tags_preservation`:** 2
- **mentions of `tags_permission`:** 3
- **mentions of `tags_keywords`:** 3
- **mentions of `tags_song_reference`:** 3

### Full source (for the brief to reference)

```python
"""
imgserver_extensions.py
=======================
Two handler functions that extend imgserver.py without rewriting it.

  1. handle_artifact_register(handler) — POST /api/artifact-register
       Creates an artifacts row pointing at any on-disk path. Bypasses
       the image-only ingest pipeline. Used for HTML page saves, audio,
       JSON, and any other pre-curated artifact.

  2. handle_asset_raw(handler) — GET /asset-raw?path=<absolute>
       Serves an arbitrary local file with a best-effort Content-Type.
       The /image-raw route already does this for images; this is its
       generalized twin.

INTEGRATION (in imgserver.py):

  Add at top, with other imports:
      from imgserver_extensions import (
          handle_artifact_register,
          handle_asset_raw,
          DB_PATH as _EXT_DB_PATH,  # unused but imported for symmetry
      )

  Add in do_POST before the 404 fallthrough:
      if p.path == "/api/artifact-register":
          return handle_artifact_register(self)

  Add in do_GET before the 404 fallthrough:
      if p.path == "/asset-raw":
          return handle_asset_raw(self)

Safety properties:
- handle_asset_raw refuses to serve any path that isn't under C:\AI\ — this
  prevents a malicious UI injection from dumping arbitrary filesystem contents.
- handle_artifact_register validates enum values against the artifacts schema
  before inserting, so bad data can't poison the DB.
"""

from __future__ import annotations
import json
import mimetypes
import os
import sqlite3
from pathlib import Path
from urllib.parse import urlparse, parse_qs

# Same path imgserver.py uses. Kept in sync by convention.
BASE = Path(r"C:\AI\Platform\MediaVault")
DB_PATH = BASE / "core" / "mediavault.sqlite"

# Allowed filesystem roots for /asset-raw. Absolute, normalized.
# Anything outside this tree is refused 403 regardless of what the UI asks for.
ASSET_ROOTS = [
    Path(r"C:\AI").resolve(),
]

# Enum whitelists mirroring the artifacts table CHECK constraints.
# Kept here so we can validate before INSERT rather than letting SQLite
# throw an opaque IntegrityError back to the client.
DOMAIN_ENUM        = {"hunter_root", "genealogy", "personal"}
SOURCE_PLATFORM    = {"instagram", "youtube", "facebook", "bandcamp",
                      "press", "local", "other", "reverbnation"}
MEDIA_TYPE         = {"photo", "video", "link", "text-only", "artwork", "mixed"}
LINK_STATUS        = {"live", "dead", "local-only"}
POST_DATE_CONF     = {"extracted", "manual", "estimated", "unknown"}
INGEST_SOURCE      = {"screenshot-pipeline", "local-drop", "url-entry",
                      "csv-import", "extension-capture"}


# ------------------------------------------------------------------
# Helpers
# ------------------------------------------------------------------

def _json_response(handler, status, payload):
    body = json.dumps(payload).encode("utf-8")
    handler.send_response(status)
    handler.send_header("Content-Type", "application/json")
    handler.send_header("Content-Length", str(len(body)))
    handler.end_headers()
    handler.wfile.write(body)


def _path_is_inside(child: Path, roots: list[Path]) -> bool:
    """True iff child resolves to a path beneath one of the allowed roots."""
    try:
        resolved = child.resolve()
    except Exception:
        return False
    for r in roots:
        try:
            resolved.relative_to(r)
            return True
        except ValueError:
            continue
    return False


def _next_artifact_id(conn: sqlite3.Connection, domain: str) -> str:
    """Match the /api/next-id route's ID format: MV-<PFX>-YYYYMMDD-NNN."""
    from datetime import date
    prefix = {"hunter_root": "HR", "genealogy": "GN", "personal": "PE"}.get(domain, "XX")
    ds = date.today().strftime("%Y%m%d")
    conn.execute(
        "INSERT INTO id_sequence(domain,date_str,last_seq) VALUES(?,?,1) "
        "ON CONFLICT(domain,date_str) DO UPDATE SET last_seq=last_seq+1",
        [domain, ds],
    )
    seq = conn.execute(
        "SELECT last_seq FROM id_sequence WHERE domain=? AND date_str=?",
        [domain, ds],
    ).fetchone()[0]
    return f"MV-{prefix}-{ds}-{str(seq).zfill(3)}"


def _infer_media_type(path: Path) -> str:
    """Guess media_type_in_post from file extension when caller didn't specify."""
    ext = path.suffix.lower()
    if ext in {".jpg", ".jpeg", ".png", ".gif", ".webp",
               ".heic", ".heif", ".bmp", ".tiff", ".tif"}:
        return "photo"
    if ext in {".mp4", ".mov", ".avi", ".mkv"}:
        return "video"
    if ext in {".mp3", ".wav", ".flac", ".m4a", ".ogg"}:
        # The artifacts enum has no 'audio'. Closest legal value is 'mixed';
        # the UI reads local_asset_path extension to render an <audio> tag.
        return "mixed"
    if ext in {".html", ".htm", ".pdf"}:
        return "link"
    return "text-only"


# ------------------------------------------------------------------
# POST /api/artifact-register
# ------------------------------------------------------------------

def handle_artifact_register(handler) -> None:
    """
    Body JSON fields:
      REQUIRED:
        local_asset_path   absolute path to the file on disk
        domain             one of DOMAIN_ENUM

      OPTIONAL (passed through if present, else sensible default):
        id                 artifact ID (auto-assigned if missing)
        ingest_source      default 'url-entry'
        source_url         canonical URL this asset represents
        source_platform    one of SOURCE_PLATFORM
        media_type_in_post one of MEDIA_TYPE; inferred from extension if missing
        link_status        one of LINK_STATUS; default 'local-only' since the
                           file lives on disk
        post_date          YYYY-MM-DD
        post_date_confidence  one of POST_DATE_CONF; default 'unknown'
        description_short  short description
        description_long   long description
        extracted_text     any text extracted from the artifact
        author_name
        tags_year_era, tags_content_type, tags_subject, tags_topic,
        tags_release_stage, tags_rarity, tags_permission,
        tags_song_reference, tags_keywords
        notes
    """
    try:
        length = int(handler.headers.get("Content-Length", 0))
        body = json.loads(handler.rfile.read(length)) if length else {}
    except Exception as e:
        return _json_response(handler, 400, {"ok": False, "error": f"bad json: {e}"})

    # --- Required fields --------------------------------------------------
    local_path_raw = body.get("local_asset_path")
    if not local_path_raw:
        return _json_response(handler, 400,
            {"ok": False, "error": "local_asset_path is required"})

    local_path = Path(local_path_raw)
    if not local_path.exists() or not local_path.is_file():
        return _json_response(handler, 400,
            {"ok": False, "error": f"file not found: {local_path_raw}"})
    if not _path_is_inside(local_path, ASSET_ROOTS):
        return _json_response(handler, 403,
            {"ok": False, "error": "local_asset_path outside allowed roots"})

    domain = body.get("domain")
    if domain not in DOMAIN_ENUM:
        return _json_response(handler, 400,
            {"ok": False, "error": f"domain must be one of {sorted(DOMAIN_ENUM)}"})

    # --- Enum-validated optionals (reject bad values instead of silently null)
    def validated(key, enum, default=None):
        v = body.get(key, default)
        if v is None:
            return None
        if v not in enum:
            raise ValueError(f"{key}={v!r} not in {sorted(enum)}")
        return v

    try:
        ingest_source        = validated("ingest_source",      INGEST_SOURCE,  "url-entry")
        source_platform      = validated("source_platform",    SOURCE_PLATFORM)
        media_type_in_post   = body.get("media_type_in_post") or _infer_media_type(local_path)
        if media_type_in_post not in MEDIA_TYPE:
            raise ValueError(f"media_type_in_post={media_type_in_post!r} not in {sorted(MEDIA_TYPE)}")
        link_status          = validated("link_status",        LINK_STATUS,    "local-only")
        post_date_confidence = validated("post_date_confidence", POST_DATE_CONF, "unknown")
    except ValueError as e:
        return _json_response(handler, 400, {"ok": False, "error": str(e)})

    # --- DB insert --------------------------------------------------------
    from datetime import date
    today = date.today().isoformat()

    try:
        conn = sqlite3.connect(str(DB_PATH), timeout=10)
        try:
            artifact_id = body.get("id") or _next_artifact_id(conn, domain)

            conn.execute(
                """INSERT OR REPLACE INTO artifacts(
                    id, domain, ingest_date, ingest_source, capture_date,
                    description_short, description_long, extracted_text,
                    author_name, post_date, post_date_confidence,
                    source_platform, media_type_in_post, link_status,
                    source_url, local_asset_path,
                    tags_year_era, tags_content_type, tags_subject, tags_topic,
                    tags_release_stage, tags_rarity, tags_preservation,
                    tags_permission, tags_song_reference, tags_keywords,
                    notes, confidence_flags
                ) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)""",
                [
                    artifact_id, domain, today, ingest_source, today,
                    body.get("description_short"),
                    body.get("description_long"),
                    body.get("extracted_text"),
                    body.get("author_name"),
                    body.get("post_date"),
                    post_date_confidence,
                    source_platform,
                    media_type_in_post,
                    link_status,
                    body.get("source_url"),
                    str(local_path.resolve()),
                    body.get("tags_year_era"),
                    body.get("tags_content_type"),
                    body.get("tags_subject"),
                    body.get("tags_topic"),
                    body.get("tags_release_stage"),
                    body.get("tags_rarity"),
                    "standard",   # tags_preservation mirrors /api/artifact-save
                    body.get("tags_permission"),
                    body.get("tags_song_reference"),
                    body.get("tags_keywords"),
                    body.get("notes"),
                    None,
                ],
            )
            conn.commit()
            return _json_response(handler, 200, {"ok": True, "id": artifact_id})
        finally:
            conn.close()
    except Exception as e:
        return _json_response(handler, 500, {"ok": False, "error": str(e)})


# ------------------------------------------------------------------
# GET /asset-raw?path=<absolute>
# ------------------------------------------------------------------

def handle_asset_raw(handler) -> None:
    """Serve any file on disk under the allowed roots, with a best-effort
    Content-Type. Range requests are supported so <audio> scrubbing works."""
    query = parse_qs(urlparse(handler.path).query)
    raw = query.get("path", [""])[0]
    if not raw:
        handler.send_error(400, "path required")
        return

    p = Path(raw)
    if not p.exists() or not p.is_file():
        handler.send_error(404, "not found")
        return
    if not _path_is_inside(p, ASSET_ROOTS):
        handler.send_error(403, "path outside allowed roots")
        return

    ctype, _ = mimetypes.guess_type(p.name)
    if not ctype:
        ctype = "application/octet-stream"

    size = p.stat().st_size
    range_header = handler.headers.get("Range", "")
    start, end = 0, size - 1
    status = 200

    if range_header.startswith("bytes="):
        # Minimal single-range support: "bytes=START-" or "bytes=START-END"
        try:
            spec = range_header[len("bytes="):]
            s, _, e = spec.partition("-")
            if s:
                start = int(s)
            if e:
                end = int(e)
            if start > end or end >= size:
                handler.send_error(416, "range not satisfiable")
                return
            status = 206
        except ValueError:
            handler.send_error(400, "bad Range header")
            return

    length = end - start + 1
    handler.send_response(status)
    handler.send_header("Content-Type", ctype)
    handler.send_header("Content-Length", str(length))
    handler.send_header("Accept-Ranges", "bytes")
    if status == 206:
        handler.send_header("Content-Range", f"bytes {start}-{end}/{size}")
    handler.end_headers()

    with open(p, "rb") as f:
        if start:
            f.seek(start)
        remaining = length
        chunk = 64 * 1024
        while remaining > 0:
            buf = f.read(min(chunk, remaining))
            if not buf:
                break
            handler.wfile.write(buf)
            remaining -= len(buf)
```

## 14. `mediavault.html` line references for the v0.5 brief

| target | first line match |
|---|---|
| ID field | 266 |
| Short description field | 270 |
| Long description field | 274 |
| Extracted text field | 278 |
| Author field | 282 |
| Notes field | 286 |
| Tag picker container | 339 |
| Applied pills container | 340 |
| Parent artifact input | 351 |
| Inbox action bar container | 354 |
| Scrap button onclick | 355 |
| Save button onclick | 356 |
| Release button onclick | 357 |
| Inbox save function | 652 |
| Inbox scrap function | 639 |
| Vault tri-state cycle fn | 869 |
| FILTER_TAG_STATE global | 470 |
| Tag chip click handler | 773 |
| renderTagBrowse fn | 750 |
| renderAppliedPills fn | 723 |
| Detail panel render fn | 1017 |
| addAppliedTag fn (group excl) | 732 |

### 14a. Inbox right-panel section headers in order (top-to-bottom)

- L263: `Identity`
- L289: `Source`
- L300: `Dates`
- L321: `Storage`
- L338: `Tags`
- L348: `Structure`

## 15. `imgserver.py` — save-and-release behavior (queue-row cleanup check)


### Route table (`/api/...` string literals)

- `/api/artifact-archive`
- `/api/artifact-delete`
- `/api/artifact-register`
- `/api/artifact-release`
- `/api/artifact-requeue`
- `/api/artifact-save`
- `/api/artifact-unrelease`
- `/api/artifact-update`
- `/api/enrich`
- `/api/fb-candidate-save`
- `/api/fb-candidates`
- `/api/intake-from-fb-candidate`
- `/api/intake-upload`
- `/api/intake-url`
- `/api/next-id`
- `/api/queue`
- `/api/queue-delete`
- `/api/queue-update`
- `/api/tag-accept`
- `/api/tag-create`
- `/api/tag-delete`
- `/api/tag-reject`
- `/api/tag-update`
- `/api/tags`
- `/api/thumbgen`

### Likely save-and-release handler (function containing artifact-save logic)

```python

def handle_tag_delete(h: BaseHTTPRequestHandler) -> None:
    body = read_body(h)
    slug = body.get("slug")
    if not slug:
        return send_error(h, 400, "slug required")
    conn = db_conn()
    try:
        row = conn.execute("SELECT usage_count FROM tags WHERE slug=?", (slug,)).fetchone()
        if not row:
            return send_error(h, 404, "tag not found")
        if row[0] != 0:
            return send_error(h, 409, f"tag in use ({row[0]} artifacts)")
        conn.execute("DELETE FROM tags WHERE slug=?", (slug,))
        conn.commit()
        send_json(h, 200, {"ok": True, "slug": slug})
    finally:
        conn.close()


def handle_fb_candidate_save(h: BaseHTTPRequestHandler) -> None:
    body = read_body(h)
    candidates = body.get("candidates")
    if candidates is None:
        return send_error(h, 400, "candidates required")
    FB_CANDIDATES_JSON.write_text(json.dumps(candidates, indent=2), encoding="utf-8")
    send_json(h, 200, {"ok": True, "count": len(candidates) if isinstance(candidates, list) else 0})


# ---------------------------------------------------------------------------
# Route table
# ---------------------------------------------------------------------------

GET_ROUTES = {
    "/":                       handle_root,
    "/ping":                   handle_ping,
    "/db":                     handle_db,
    "/fb":                     handle_fb_html,
    "/api/queue":              handle_queue_list,
    "/api/tags":               handle_tags_list,
    "/api/fb-candidates":      handle_fb_candidates_get,
    "/ext/hr_manager_renderer.js": handle_renderer_js,
}

POST_ROUTES = {
    "/api/intake-upload":            handle_intake_upload,
    "/api/intake-url":               handle_intake_url,
    "/api/intake-from-fb-candidate": handle_intake_from_fb_candidate,
    "/api/queue-update":             handle_queue_update,
    "/api/queue-delete":             handle_queue_delete,
    "/api/enrich":                   handle_enrich,
    "/api/next-id":                  handle_next_id,
    "/api/artifact-save":            handle_artifact_save,
    "/api/artifact-update":          handle_artifact_update,
    "/api/artifact-release":         handle_artifact_release,
    "/api/artifact-unrelease":       handle_artifact_unrelease,
    "/api/artifact-archive":         handle_artifact_archive,
    "/api/artifact-delete":          handle_artifact_delete,
    "/api/artifact-requeue":         handle_artifact_requeue,
    "/api/artifact-register":        handle_artifact_register,   # from imgserver_extensions
    "/api/thumbgen":                 handle_thumbgen,
    "/api/tag-create":               handle_tag_create,
    "/api/tag-update":               handle_tag_update,
    "/api/tag-accept":               handle_tag_accept,
    "/api/tag-reject":               handle_tag_reject,
    "/api/tag-delete":               handle_tag_delete,
    "/api/fb-candidate-save":        handle_fb_candidate_save,
}

# Prefix-matched routes (path starts with these — extension via query string).
GET_PREFIX_ROUTES = [
    ("/image-raw", handle_image_raw),
    ("/asset-raw", handle_asset_raw),
]

# ROUTES totals (kept verifiable by code, not just comments):
ROUTE_COUNT = (len(GET_ROUTES) + len(POST_ROUTES) + len(GET_PREFIX_ROUTES))


# ---------------------------------------------------------------------------
# Handler class
# ---------------------------------------------------------------------------

class H(BaseHTTPRequestHandler):
    server_version = "MediaVault/0.4"

    def log_message(self, fmt, *args):  # noqa: D401
        # Quieter access log, prefixed for grep.
        sys.stdout.write("[mv] " + (fmt % args) + "\n")

    # CORS preflight (used by the optional capture extension)
    def do_OPTIONS(self):
        self.send_response(204)
        self.send_header("Access-Control-Allow-Origin", "*")
        self.send_header("Access-Control-Allow-Headers", "Content-Type")
        self.send_header("Access-Control-Allow-Methods", "GET,POST,OPTIONS")
        self.end_headers()

    def _dispatch(self, table, prefix_table=None):
        path = urlparse(self.path).path
        fn = table.get(path)
        if fn is not None:
            try:
                fn(self)
            except Exception as e:
                send_error(self, 500, f"handler error: {e}")
            return
        if prefix_table:
            for prefix, fn in prefix_table:
                if path == prefix or path.startswith(prefix + "?") or path.startswith(prefix + "/"):
                    try:
                        fn(self)
                    except Exception as e:
                        send_error(self, 500, f"handler error: {e}")
                    return
        send_error(self, 404, f"no route: {path}")

    def do_GET(self):
        self._dispatch(GET_ROUTES, GET_PREFIX_ROUTES)

    def do_POST(self):
        self._dispatch(POST_ROUTES)


# ---------------------------------------------------------------------------
# Entry point
# -------------------------------------------------------------
```

### All references to `ingest_queue` (SELECT/UPDATE/DELETE/INSERT)

- `SELECT * FROM ingest_queue WHERE status=? ORDER BY queue_id DESC`
- `SELECT * FROM ingest_queue ORDER BY queue_id DESC LIMIT 500`
- `INSERT INTO ingest_queue(ingest_source, raw_path, queued_at, status, updated_at)`
- `INSERT INTO ingest_queue(ingest_source, source_url, queued_at, status,`
- `INSERT INTO ingest_queue(ingest_source, source_url, raw_path,`
- `UPDATE ingest_queue SET {`
- `DELETE FROM ingest_queue WHERE queue_id=?`
- `SELECT * FROM ingest_queue WHERE queue_id=?`
- `UPDATE ingest_queue SET enrichment_json=?, status=`
- `UPDATE ingest_queue SET artifact_id=?, status=`
- `INSERT INTO ingest_queue(ingest_source, raw_path, source_url, queued_at,`

### `release_immediately` usage sites

- L692: `release_now = bool(body.get("release_immediately"))`

## 16. Frontend → backend route cross-reference


### Called by mediavault.html

- `/api/artifact-archive` — ✓
- `/api/artifact-release` — ✓
- `/api/artifact-requeue` — ✓
- `/api/artifact-save` — ✓
- `/api/artifact-unrelease` — ✓
- `/api/artifact-update` — ✓
- `/api/enrich` — ✓
- `/api/next-id` — ✓
- `/api/queue` — ✓
- `/api/queue-update` — ✓
- `/api/tag-accept` — ✓
- `/api/tag-create` — ✓
- `/api/tag-delete` — ✓
- `/api/tag-reject` — ✓
- `/api/tag-update` — ✓
- `/api/tags` — ✓

### Served by imgserver.py but never called by the frontend

- `/api/artifact-delete`
- `/api/artifact-register`
- `/api/fb-candidate-save`
- `/api/fb-candidates`
- `/api/intake-from-fb-candidate`
- `/api/intake-upload`
- `/api/intake-url`
- `/api/queue-delete`
- `/api/thumbgen`